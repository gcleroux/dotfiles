# Installing custom neovim instance

## Build prerequisites

```bash
sudo apt update
sudo apt install curl ripgrep git
```

## Installing neovim

```bash
git clone https://github.com/gcleroux/dotfiles.git
cd dotfiles/
sudo bash ./install.sh
```
